[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: fast tests, no external dependencies
    integration: requires Redis + PostgreSQL (testcontainers)
    load: performance tests, run separately
